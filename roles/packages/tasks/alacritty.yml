---
- name: Create config dir
  ansible.builtin.file:
    path: "{{ packages_alacritty_config_dir }}"
    state: directory

- name: Copy config file
  ansible.builtin.template:
    src: alacritty.toml.j2
    dest: "{{ packages_alacritty_config_dir }}/alacritty.toml"
